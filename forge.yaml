objective: "Create a simple Rust program in src/main.rs that prints a short haiku about Rust state machines."
artifact:
  repo_path: ".forge/artifacts/main.git"
  branch: "main"
provider:
  base_url: "http://localhost:8080"
  n_predict: 512
telemetry:
  directory: "runs"
project:
  kind: coding
