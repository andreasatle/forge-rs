objective: "Create a simple Python program in main.py that implements a Fibonacci number generator. The program should expose a reusable fibonacci(n: int) function and include corresponding unit tests."
artifact:
  repo_path: ".forge/artifacts/main.git"
  branch: "main"
provider:
  cheap:
    managed:
      llama_cpp:
        command: llama-server
        model:
          hf: lm-kit/qwen-3-14b-instruct-gguf:Q4_K_M
        host: 127.0.0.1
        port: 8080
        n_predict: 1024
telemetry:
  directory: "runs"
project:
  kind: coding
  language: python
